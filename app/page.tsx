import Board from '@/components/Board';

const Home = () => {
  return (
    <main className='gradient max-sm:w-full max-sm:min-h-screen md:h-screen md:w-screen max-sm:overflow-auto'>
      <Board />
    </main>
  );
};

export default Home;
